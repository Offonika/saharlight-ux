{
  "name": "diabetes-assistant-root",
  "private": true,
  "dependencies": {},
  "scripts": {
    "setup": "npm --prefix apps/web ci",
    "dev": "npm run setup && npm --prefix apps/web run dev",
    "build": "npm run setup && npm --prefix apps/web run build",
    "build:dev": "npm run setup && npm --prefix apps/web run build:dev",
    "preview": "npm --prefix apps/web run preview",
    "lint": "npm --prefix apps/web run lint"
  }
}
