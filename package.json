
{
  "name": "saharlight-monorepo",
  "private": true,
  "workspaces": ["services/webapp/ui", "libs/ts-sdk"],
  "scripts": {
    "dev":       "npm --workspace services/webapp/ui run dev",
    "build":     "npm --workspace services/webapp/ui run build",
    "preview":   "npm --workspace services/webapp/ui run preview",
    "generate:sdk": "npx @openapitools/openapi-generator-cli generate -i libs/contracts/openapi.yaml -g typescript-fetch -o libs/ts-sdk -p useSingleRequestParameter=true && npx @openapitools/openapi-generator-cli generate -i libs/contracts/openapi.yaml -g python -o libs/py-sdk -p packageName=diabetes_sdk,modelPropertyNaming=camelCase,snakeCaseIfNeed=false"
  }
}
