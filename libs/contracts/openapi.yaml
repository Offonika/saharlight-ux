openapi: 3.0.3
info:
  title: Diabetes Assistant API
  version: 1.0.0
paths:
  /health:
    get:
      operationId: healthGet
      summary: Health check
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Status'
  /timezone:
    post:
      operationId: timezonePost
      summary: Save timezone
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Timezone'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Status'
  /profiles:
    get:
      operationId: profilesGet
      summary: Get user profile
      parameters:
        - in: query
          name: telegram_id
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Profile'
    post:
      operationId: profilesPost
      summary: Save user profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Profile'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Status'
  /api/reminders:
    get:
      operationId: remindersGet
      summary: List or retrieve reminders
      parameters:
        - in: query
          name: telegram_id
          required: true
          schema:
            type: integer
        - in: query
          name: id
          schema:
            type: integer
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/Reminder'
                  - type: array
                    items:
                      $ref: '#/components/schemas/Reminder'
    post:
      operationId: remindersPost
      summary: Save reminder
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Reminder'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  id:
                    type: integer
components:
  schemas:
    Status:
      type: object
      properties:
        status:
          type: string
      required:
        - status
    Timezone:
      type: object
      properties:
        telegram_id:
          type: integer
        tz:
          type: string
      required:
        - telegram_id
        - tz
    Profile:
      type: object
      properties:
        telegram_id:
          type: integer
        icr:
          type: number
          format: float
        cf:
          type: number
          format: float
        target:
          type: number
          format: float
        low:
          type: number
          format: float
        high:
          type: number
          format: float
        org_id:
          type: integer
          nullable: true
      required:
        - telegram_id
        - icr
        - cf
        - target
        - low
        - high
    Reminder:
      type: object
      properties:
        telegram_id:
          type: integer
        id:
          type: integer
        type:
          type: string
        time:
          type: string
        interval_hours:
          type: integer
          nullable: true
        is_enabled:
          type: boolean
      required:
        - telegram_id
        - type
